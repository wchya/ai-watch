# 定时可靠性摘要实施计划

1. 扩展全局 Settings、默认值、校验和 schema v11 迁移。
2. 实现摘要生成、定时循环、持久化去重、失败事件和手动发送。
3. 暴露预览与发送 API，并接入统一幂等中间件。
4. 在设置页添加主题化配置卡、操作 loading 和 Markdown 预览。
5. 更新 API mock 与端到端测试，补充 Go 单元/集成测试。
6. 运行 Go、前端构建、E2E 和差异检查，重新构建容器并核验健康状态。

