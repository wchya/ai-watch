# 首页可用性与行动中心实施计划

1. 新增独立 `DashboardActionCenter` 组件。
2. 并行加载事故、计划和可靠性数据，使用 `Promise.allSettled` 局部降级。
3. 组合 Dashboard 活动任务和 Provider 快照，生成按严重度排序的行动项。
4. 接入事故、请求、计划路由以及测活、停止任务操作。
5. 增加语义主题、桌面双栏和移动端布局。
6. 扩展 E2E Mock，验证聚合、局部错误和快捷操作。
7. 全量测试后重新部署。
