# 通知路由中心实施计划

1. 增加通知渠道与路由模型、SQLite v17 和 Redis 加密存储。
2. 实现按消息类型发送、默认回退、去重和结构化审计。
3. 将事故、可靠性告警、可靠性恢复、摘要和任务通知接入路由类型。
4. 增加渠道 CRUD、测试和路由配置 API。
5. 新增 `/notification-routing` 页面、侧边栏入口和语义主题样式。
6. 增加 Store、路由服务、API 和 Playwright 测试。
7. 运行全量回归、构建镜像、部署并只读冒烟。
