# 事故复盘报告实施计划

1. 增加 IncidentPostmortem 模型和 SQLite/Redis Store。
2. 实现从 Incident 生成稳定脱敏快照和 Markdown。
3. 增加生成、读取、保存、完成、重开和 Markdown API。
4. 在事故详情接入复盘抽屉和编辑操作。
5. 增加语义主题、极昼主题与移动端样式。
6. 增加 Store、API 和 Playwright 验收测试。
7. 运行全量测试、构建镜像、部署并执行只读冒烟。
