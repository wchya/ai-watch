# AI Watch 核心产品收拢实施计划

**状态：已实施，等待最终容器部署验收。**

## 阶段 1：删除低价值能力

1. 删除供应商示例前端、API、Store、类型和测试。
2. 删除 Redis 管理页面、API、类型和测试。
3. 增加 SQLite/Redis 清理逻辑并验证核心数据不受影响。

## 阶段 2：领域中心

1. 新建路由注册表和 7 个一级入口。
2. 验证中心合并场景与对比历史。
3. 自动化合并计划、故障切换与维护窗口。
4. 稳定性合并可靠性、SLO、事故与复盘。
5. 设置合并通知路由与诊断。
6. 旧 path 重定向到新中心。

## 阶段 3：工程减重

1. 拆分 `App.tsx`，抽取应用外壳、Dashboard、Settings 和路由渲染。
2. 抽取异步资源、写操作和延迟刷新 hooks。
3. 拆分 API 路由入口并统一错误与审计辅助函数。
4. 删除重复样式和旧兼容分支。

## 阶段 4：验证与同步

1. 重写 E2E 为 7 个领域核心流程。
2. 运行全量 Go、构建、E2E 和 `git diff --check`。
3. 使用 code-reviewer 审查本地改动。
4. 使用 neat-freak 同步 README/docs/规则并审计文档体积。
5. 构建部署并执行只读数据与路由冒烟。
