# Provider Group SLO 与错误预算实施计划

1. 扩展 Provider Group SLO 配置字段及 Store 校验和往返测试。
2. 新增 SLO 统计服务，按 Group 绑定请求计算滚动窗口指标并排除活动维护窗口样本。
3. 新增 SLO 列表、配置、暂停和恢复 API，写结构化审计事件。
4. 新增前端类型、API、`/slos` 路由、侧边栏入口和 SLO 页面。
5. 增加语义主题、极昼主题与移动端样式。
6. 增加后端计算/API 测试和 Playwright 验收 Mock。
7. 运行全量测试、构建镜像、部署并执行只读冒烟。
